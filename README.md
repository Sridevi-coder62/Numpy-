# NumPy Tutorial & Practice

This repository contains my **NumPy learning and practice work completed in Google Colab**. It covers the fundamental concepts of NumPy, including creating arrays, indexing, slicing, reshaping, iterating, joining, searching, sorting, and filtering arrays.

The exercises and examples in this repository are based on the core NumPy concepts covered in the tutorial.

## 📚 Topics Covered

### 1. NumPy Introduction

- Introduction to NumPy
- Why NumPy is used
- Advantages of NumPy over Python lists
- Importing NumPy

### 2. Getting Started

- Installing/importing NumPy
- Creating a NumPy array
- Checking NumPy version
- Basic NumPy operations

### 3. Creating Arrays

- Creating 1-D arrays
- Creating 2-D arrays
- Creating arrays with different dimensions
- Using `ndmin`
- Creating arrays with specific data types

### 4. Array Indexing

- Accessing elements from 1-D arrays
- Accessing elements from 2-D arrays
- Positive and negative indexing
- Accessing specific rows and columns

### 5. Array Slicing

- Slicing 1-D arrays
- Slicing 2-D arrays
- Start, stop and step
- Negative slicing
- Selecting specific rows and columns

### 6. Data Types

- Understanding NumPy data types
- Checking data types using `dtype`
- Creating arrays with a specific data type
- Integer, float and string data types
- Understanding type codes such as `i4` and `S`

### 7. Copy vs View

- Difference between NumPy Copy and View
- Creating a copy using `copy()`
- Creating a view using `view()`
- Understanding how changes affect the original array

### 8. Array Shape

- Understanding array dimensions
- Using the `shape` attribute
- Finding rows and columns
- Working with 1-D, 2-D and higher-dimensional arrays

### 9. Array Reshape

- Reshaping arrays using `reshape()`
- Converting 1-D arrays into 2-D arrays
- Working with different dimensions
- Understanding the relationship between array size and shape

### 10. Array Iterating

- Iterating through NumPy arrays
- Using `for` loops
- Iterating through 1-D and 2-D arrays
- Using `np.nditer()`
- Iterating through selected elements using slicing

### 11. Array Join

- Joining NumPy arrays
- Using `concatenate()`
- Joining arrays along different axes
- Working with 1-D and 2-D arrays

### 12. Array Split

- Splitting NumPy arrays
- Using `array_split()`
- Using `split()`
- Splitting arrays into multiple parts

### 13. Array Search

- Searching for elements in an array
- Using `where()`
- Finding positions/indexes of elements
- Using `searchsorted()`
- Understanding insertion positions in sorted arrays

### 14. Array Sort

- Sorting NumPy arrays
- Using `sort()`
- Sorting 1-D arrays
- Sorting 2-D arrays

### 15. Array Filter

- Filtering NumPy arrays
- Using Boolean conditions
- Creating Boolean index lists
- Selecting elements based on conditions

## 🛠️ Tools Used

- **Python**
- **NumPy**
- **Google Colab**

## 🎯 Learning Objectives

Through this practice, I learned how to:

Create and manipulate NumPy arrays
Access and slice array elements
Understand NumPy data types
Work with array dimensions and shapes
Reshape arrays
Iterate through arrays
Join and split arrays
Search for elements and positions
Sort arrays
Filter array elements using conditions

## 🚀 How to Run

The exercises were completed using Google Colab.

To practice the notebooks:

Open the notebook in Google Colab.
Run the cells step by step.
Modify the examples and experiment with different arrays.
Observe the output and understand how NumPy operations work.

## 📌 Conclusion

This repository represents my hands-on practice with the fundamentals of NumPy. These concepts form an important foundation for further learning in Python, Data Analysis, Data Science, Machine Learning, and Artificial Intelligence.
